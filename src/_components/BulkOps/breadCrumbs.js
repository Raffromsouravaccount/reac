import {
  constantText
} from '../../_helpers/constants.text'

export const breadCrumbs = {
  links: [{
      color: 'inherit',
      text: constantText.dashBoard_text,
      route: '/dashboard'
    }
  ],
  typography:   [{
    color: 'textPrimary',
    text: constantText.bulksOpsConstant.bulk_title
  }]
};

